# claude-chart 백테스트 신뢰도 개선 계획

## 범위

기존 BTCUSDT 5년·1H 원자료와 1H/4H/12H/1D 분석 엔진을 재검증한다. 차트 레이아웃·저장 설정은 유지하며, 수익률을 좋게 보이게 하는 사후 튜닝은 하지 않는다.

## 순서

1. 기존 파일을 별도 백업하고 현재 테스트를 실행한다. 공식 Binance·MEXC 확정봉을 수집해 수정 전 기준선을 저장한다.
2. 학습·검증·시험 경계의 미래 수익률 혼입, 누락 봉의 경과 시간 오류, 갭 손절, 균형 정확도 정의를 테스트로 재현하고 수정한다.
3. 같은 확정봉을 반복 분석하는 부분만 캐시해 속도를 개선한다. 캐시 없는 결과와 수치가 일치하는지 비교한다.
4. 화면도 250개 확정봉을 사용하도록 입력 수를 맞추고, 백테스트 요약을 보고서에서 자동 생성한다. 미검증 종목은 관망 표시를 유지한다.
5. 수수료·슬리피지 반영 결과, 데이터 결측, 표본 의존성과 반복 검증 한계를 보고한다. 회귀 테스트와 실제 브라우저 확인 후 선별 커밋·배포한다.

## 검증 기준

- 후보 선택은 학습 기간에서만 수행한다. 각 구간 끝 이후의 가격은 사용하지 않는다.
- 12H 방향 정확도와 손절·목표 전략의 순승률은 별개로 보고한다.
- 균형 정확도는 실제 상승·하락별 재현율의 평균이다. 롱·숏 적중률 평균과 구분한다.
- 누락 구간의 가격은 보간하지 않는다. 미래 평가에 연속된 시간이 확보되지 않는 표본은 제외한다.
- 반복되는 과거 자료의 검증이며 새로운 미관측 표본 검증이 아니다. 펀딩비·호가·부분체결은 미반영한다.

## 실행 결과 (2026-09-06 KST)

- 수집 기간: 2021-09-05 14:00 UTC부터 2026-09-05 14:00 UTC 미만. Binance 43,824봉, MEXC 43,819봉. MEXC 5시간 누락과 거래량 0인 1봉은 보고서에 명시했다.
- 최종 시험 후보: 396신호, 12H 방향 적중률 54.55%, 균형 정확도 53.70%. 단순 Wilson 95% 구간은 49.62~59.38%이며 50%를 포함한다. 표본이 시간상 겹치므로 독립 시행 신뢰구간으로 해석하지 않는다.
- 비용 반영 12H 고정 보유: 137거래, 순승률 51.82%, PF 0.669, 거래당 평균 -0.281%. 후보는 채택하지 않고 화면 관망을 유지했다.
- 같은 최신 스냅샷의 수정 전후 후보 선택·시험 적중률·고정 보유 손익은 같았다. 균형 정확도는 정의를 바로잡아 54.08%에서 53.70%로 바뀌었다. 이를 수익성 개선이라고 주장하지 않는다.
- 중복 계산 캐시 검증: 9,000개 원자료 봉에서 생성한 742개 판정의 모든 필드가 캐시 없이 계산한 결과와 일치했다. 두 측정에서 701.0→513.5ms, 706.6→536.1ms로 약 24~27% 단축됐다. 전체 페이지 로딩 속도 측정이 아니다.

## 수정 로직 설명

1. 시간 경계: 신호 시각뿐 아니라 이후 최대 24시간의 평가 시각도 해당 구간 안에 들어오는지 검사한다. 학습이 다음 검증 구간의 가격을 미리 보는 오류를 방지한다.
2. 누락 시간: 앞으로 12개 봉이 있다는 것만 확인하지 않고 각 봉이 실제 1시간 간격인지 확인한다. 누락 때문에 더 먼 미래의 종가를 12시간 뒤 가격으로 사용하는 것을 막는다.
3. 손절 갭: 시가가 손절가를 이미 넘어선 경우 손절가 대신 불리한 시가를 사용한다. 실제보다 유리한 가상 체결을 줄인다.
4. 중립 점수: 가격·이동평균·DI·MACD 비교값이 같으면 하락 점수를 더하지 않는다. 완전 횡보를 약세로 잘못 판정하는 문제를 수정했다.
5. 계산 캐시: 같은 확정 상위 시간봉이 반복 사용될 때 직전 계산값을 재사용한다. 봉이 바뀌면 다시 계산하고 미래 데이터는 읽지 않는다.
6. 화면 근거: 진행 봉을 포함해 251개를 요청하여 확정 250봉을 확보한다. 보고서 생성 시 화면용 calibration.js도 함께 생성하여 과거 승률이 남는 문제를 방지한다.

## 재현

- 원자료 갱신: `node backtest/fetch_btc_5y.mjs`
- 저장 원자료 재검증: `node backtest/run_backtest.mjs`
- 무결성 회귀 검사: `node test_backtest_integrity.mjs`
- 계산 동등성·속도 검사: `node test_backtest_performance.mjs`
- 원자료는 용량 때문에 Git에서 제외하고 로컬 backtest_data에 유지했다. 보고서에 입력 파일 SHA-256을 기록했다. 재수집하면 기간·값이 달라질 수 있다.
