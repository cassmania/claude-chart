# 차트별 설정 영구 저장 구현 계획

## 목표

사용자가 각 차트에 지정한 코인과 시간봉, 화면 분할 수를 브라우저에 저장하고 다음 접속 때 동일하게 복원한다.

## 보존 범위

- 기존 V3.2 코인분석 엔진과 분석 계산은 변경하지 않는다.
- 차트 디자인, 클릭 동작, 즐겨찾기 기능을 유지한다.
- 서버나 개인정보 저장소를 추가하지 않고 현재 브라우저의 `localStorage`만 사용한다.

## 구현 단계

1. `claude_chart_state_v1`이라는 버전이 포함된 저장 키를 추가한다.
2. 다음 상태를 하나의 JSON 객체로 저장한다.
   - 현재 분할 수
   - 각 차트 위치별 코인 심볼
   - 각 차트 위치별 시간봉
   - 마지막으로 선택한 활성 차트 위치
3. 페이지 시작 시 저장값을 검증한다.
   - 허용된 분할 수와 시간봉만 복원한다.
   - 코인 심볼은 `코인/USDT` 형식만 허용한다.
   - 손상된 JSON이나 잘못된 값은 안전한 기본값으로 대체한다.
4. 코인·시간봉·분할 수·활성 차트가 바뀔 때마다 즉시 저장한다.
5. 저장 기능 실패가 차트 실행을 막지 않도록 예외를 처리한다.

## 검증 계획

- 저장값이 없을 때 기존 8분할 기본값을 사용하는지 확인한다.
- 8개 차트의 코인과 시간봉을 바꾼 뒤 새로고침해 그대로 복원되는지 확인한다.
- 분할 수를 바꾼 뒤 새로고침해 동일한 분할 수가 복원되는지 확인한다.
- 잘못된 JSON과 허용되지 않은 값이 들어와도 앱이 기본값으로 실행되는지 확인한다.
- 기존 `test_market_analyzer.js`, `test_level_analyzer.js`가 모두 통과하는지 확인한다.

## 백업

- 수정 전 원본: `E:\AI 관련 자료\챗지피티\backups\claude-chart\20260823-071503-chart-state-persistence\index.html`
- 원본 SHA-256: `E18B5F5072F2C1E169AF60B91DA5A5F99B5A717153FBE0C6567FE9D8BB90563D`
