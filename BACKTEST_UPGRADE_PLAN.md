# BTC 5년 교차 거래소 백테스트 및 정확도 개선 계획

## 1. 목표

현재 `market_analyzer.js`의 강세·약세·관망 판정을 Binance와 MEXC의 BTC USDT 무기한 선물 5년 OHLCV로 재현하고, 시간 누수와 과적합을 통제한 검증에서 실제로 개선되는 규칙만 반영한다.

## 2. 데이터 계약

- Binance: `BTCUSDT`, USDT-M perpetual, 공식 `/fapi/v1/klines`
- MEXC: `BTC_USDT`, USDT perpetual, 공식 `/api/v1/contract/kline`
- 원자료: 1H OHLCV
- 재집계: 4H, 12H, 1D UTC 확정 봉
- 기간: 실행 시점부터 과거 5년. 거래소별 실제 시작·종료 시각과 누락 봉을 보고한다.
- 로컬 캐시는 `backtest_data`에 저장하고 Git에는 포함하지 않는다.

## 3. 정확도 정의

### 1차 지표

- 매 4시간 확정 시점에 생성된 강세·약세 신호와 이후 12시간 종가 수익률 방향의 일치율
- 상승·하락 표본 불균형을 보정한 균형 정확도
- 강세 적중률, 약세 적중률, 신호 커버리지, 관망률, 표본 수
- Wilson 95% 신뢰구간

### 2차 지표

- 다음 1H 시가 진입
- 고가·저가로 목표/손절 체결 판정
- 같은 봉에서 목표와 손절이 모두 닿으면 보수적으로 손절 우선
- 최대 보유 24시간 후 종가 청산
- 수수료·슬리피지 반영 전후 기대값, Profit Factor, 최대 낙폭

## 4. 시간 누수 및 과적합 방지

1. 각 판정 시점까지 완전히 확정된 봉만 사용한다.
2. 신호를 만든 봉의 종가에는 체결하지 않고 다음 1H 시가를 사용한다.
3. Binance 앞 3년만 후보 규칙 탐색에 사용하고, 그 안을 연도별 워크포워드 폴드로 나눠 최악 연도 성능을 우선한다.
4. Binance 다음 1년은 검증, 최근 1년은 최종 시험으로 고정한다.
5. MEXC 결과는 거래소 간 재현성 확인용으로 사용하며 튜닝에 사용하지 않는다.
6. 최소 표본 수와 최소 커버리지를 충족하지 않는 고적중률 후보는 폐기한다.
7. BTC 가격 형성의 공통성 때문에 두 거래소 결과가 완전히 독립적인 시장 표본은 아님을 명시한다.

## 5. 비교 후보

- 현재 기준: 시간봉 점수 절댓값 3 이상, 4개 중 3개 방향 일치
- 점수 임계값: 3, 4, 5
- MTF 일치 수: 3개, 4개
- 4H ADX 하한: 없음, 20, 25
- 4H 거래량 비율 하한: 없음, 0.8, 1.0
- 과열 추격 방지: 롱 RSI 70 미만·숏 RSI 30 초과 적용 여부
- 1D와 4H 방향 일치 필수 여부
- 추세 상태를 미래에도 이어지는 것으로 보는 `추세 지속형`과, 과도한 다중 시간봉 합의를 되돌림 조건으로 보는 `평균회귀형`을 분리 검증

초기 실행에서 현재 강세·약세 상태를 그대로 12H 예측으로 사용한 정확도가 두 거래소 모두 50% 미만임을 확인했다. 따라서 상태 라벨은 유지하되 미래 전망과 분리하고, 4H·12H·24H에서 방향 매핑의 안정성을 추가 검증한다.

후보는 Binance 학습 구간에서만 선택하고, 검증 구간의 균형 정확도·표본 수·커버리지가 현재 기준보다 나빠지면 채택하지 않는다. 최종 시험과 MEXC에서 정확도 95% 구간 하한이 50%를 넘고, 비용 반영 기대값도 양수여야 검증형 전망으로 채택한다.

## 6. 변경 범위

- 신규 데이터 수집·백테스트 스크립트와 결과 보고서 추가
- 기존 파일은 수정 전 `backups`에 보관
- 검증을 통과한 경우에만 `market_analyzer.js`의 최종 판정 게이트를 최소 변경
- 현재 차트·매물대·기존 지표 계산은 유지
- 정적 단위 테스트, 5년 백테스트 재실행, 실제 브라우저 검증 후 커밋·푸시·Pages 배포

## 7. 완료 기준

- 거래소별 5년 데이터 범위·누락·중복·가격 이상치를 보고한다.
- 기준 엔진과 개선 엔진을 같은 데이터·같은 비용으로 비교한다.
- 학습/검증/시험/MEXC 결과를 분리해 보고한다.
- 정확도가 개선되지 않으면 수치와 이유를 공개하고 엔진을 억지로 변경하지 않는다.
- 개선 시 적용 규칙, 무효화 조건, 한계를 README와 결과 보고서에 기록한다.
