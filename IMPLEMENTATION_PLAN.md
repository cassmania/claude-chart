# 코인분석스킬 3.1 이식 구현 계획

## 목표

기존 8분할 차트와 다중 타임프레임 매물대 기능을 유지하면서, `코인분석스킬 3.1v`의 핵심 분석 규칙을 정적 GitHub Pages 환경에서 재현 가능한 계산으로 이식한다.

## 구현 범위

1. 확정 봉만 사용하는 분석 엔진을 별도 JavaScript 파일로 추가한다.
2. ADX(14), +DI/-DI, SMA 20/50/200, RSI(14), MACD(12,26,9), 거래량 상태를 계산한다.
3. 좌우 5개 확정 봉 기반 스윙, BOS, CHoCH, 유동성 스윕, FVG 후보를 판정한다.
4. 1H/4H/12H/1D 데이터를 통합해 상위 봉부터 방향을 표시한다.
5. 기존 매물대 결과와 결합해 조건부 롱·숏·관망 시나리오, 무효화 가격, 목표 구간을 표시한다.
6. 거래소·계약·기준 시각·마지막 확정 봉을 명시하고, 웹페이지에서 검증할 수 없는 온체인·뉴스·언락·청산맵은 `현재 실시간 데이터 확인 불가`로 표시한다.

## 안전 원칙

- 기존 레이아웃, 차트 렌더링, 피벗 및 매물대 엔진은 삭제하지 않는다.
- 변경 전 대상 파일을 `backups` 폴더에 시각별로 복사한다.
- 새 분석 엔진은 독립 테스트 파일로 검증한다.
- 실제 브라우저에서 콘솔 오류, API 호출, 분석 패널 렌더링을 확인한다.

## 완료 기준

- Node 단위 테스트가 모두 통과한다.
- 기존 `level_analyzer.js` 테스트 29개가 계속 통과한다.
- GitHub Pages 형태의 로컬 서버에서 분석 패널이 정상 렌더링된다.
- 변경 내용을 커밋하고 `origin/main`에 푸시한다.
