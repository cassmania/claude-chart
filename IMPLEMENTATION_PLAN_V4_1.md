# Claude Chart 코인분석스킬 V4.1 이식 계획

## 목표

- 기존 차트·상태 저장·레벨 엔진을 유지하면서 정밀 분석 패널을 V4.1 PART 0~5 체계로 업그레이드한다.
- 공식 거래소 확정봉과 실제 조회된 파생상품 데이터만 사용한다.
- 온체인·고래·청산맵·뉴스·언락처럼 수집되지 않은 값은 추정하지 않고 `현재 실시간 데이터 확인 불가`로 표시한다.

## 구현 단계

1. 변경 전 공개 루트 파일을 지정 백업 폴더에 복사한다.
2. 기존 V3.2 시장구조 엔진을 재사용하는 `v41_analyzer.js` 호환 레이어를 추가한다.
3. SMA 20/60/120/200, RSI(14), CCI(20), MACD, Stochastic(14,3,3), 앵커 VWAP, 근사 VPVR의 값과 산출 한계를 V4.1 결과에 포함한다.
4. 화면을 PART 0 데이터 기준·USDT.D, PART 1 기술적 분석, PART 2 파생상품, PART 3 온체인·고래, PART 4 토큰노믹스·뉴스, PART 5 실행전략·위험관리 순서로 표시한다.
5. USDT/KRW 환율의 출처·조회 시각과 마지막 확정봉 시각을 표시한다.
6. 기존 및 신규 단위 테스트와 문법 검사를 실행한다.
7. 390×844 모바일 브라우저에서 렌더링과 오류를 확인한다.
8. 선택 파일만 커밋·푸시하고 GitHub Pages의 캐시 무효화 URL에서 배포를 검증한다.

## 완료 기준

- 기존 기능 회귀 테스트와 V4.1 테스트가 모두 통과한다.
- V4.1 PART 0~5와 위험관리 산식이 실제 화면에 표시된다.
- 공개 배포 파일과 화면이 새 커밋을 반영한다.
